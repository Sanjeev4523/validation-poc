# Buf Schema Registry (BSR) Authentication Token
# Required for accessing private BSR repositories
# Get your token from: https://buf.build/settings/user
BUF_TOKEN=your_buf_token_here

# Logging Level
# Options: DEBUG, INFO, WARN, ERROR
# Default: INFO
LOG_LEVEL=INFO

# Schema Source Mode
# Options: local-then-bsr, bsr-only, local-only
SCHEMA_SOURCE_MODE=local-then-bsr

# Validation Source Mode
# Options: bsr-only, local-only, local-then-bsr
VALIDATION_SOURCE_MODE=bsr-only
