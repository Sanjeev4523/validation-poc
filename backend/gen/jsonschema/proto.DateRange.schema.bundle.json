{
  "$defs": {
    "proto.DateRange.schema.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "DateRange demonstrates date comparison validation",
      "patternProperties": {
        "^(endDate)$": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "pattern": "^-?[0-9]+$",
              "type": "string"
            }
          ],
          "description": "endDate must be after startDate"
        },
        "^(startDate)$": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "pattern": "^-?[0-9]+$",
              "type": "string"
            }
          ],
          "description": "startDate must be before endDate"
        }
      },
      "properties": {
        "end_date": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "pattern": "^-?[0-9]+$",
              "type": "string"
            }
          ],
          "description": "endDate must be after startDate"
        },
        "start_date": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "pattern": "^-?[0-9]+$",
              "type": "string"
            }
          ],
          "description": "startDate must be before endDate"
        }
      },
      "required": [
        "start_date",
        "end_date"
      ],
      "title": "Date Range",
      "type": "object"
    }
  },
  "$id": "proto.DateRange.schema.bundle.json",
  "$ref": "#/$defs/proto.DateRange.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
